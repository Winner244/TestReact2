# React + TypeScript + Vite + Less

use to start:
1. npm install
2. npm run dev
3. go to http://localhost:5173/ in your browser


8h are spent.
All bonuses are implemented:
1. persist filters to URL query params and restore on reload
2. on mobile, render the sidebar as a left Drawer
3. edit the description with an RTE (TipTap)

Link: https://jolly-figolla-2c3ea1.netlify.app/